# ORACLE deployment and recovery

GitHub source control and Railway application/PostgreSQL hosting. Recorded September 6, 2026. Batch 11 v0.11.0/schema 10, release `6127db477f76f1c3b9f18ea6eb423e029c9dcaf7`, is deployed. Both exact-commit PostgreSQL CI runs, separate isolated 100-player measurements, Railway staging, the complete all-twelve/all-three-theme remote workflow, and Railway production passed. Exact production readiness and 70 public GET checks passed without gameplay writes. Actual human/device acceptance remains unrun.

## Current infrastructure

Source: [GreenShoeGarage/Oracle](https://github.com/GreenShoeGarage/Oracle). Checked staging/production release: `6127db477f76f1c3b9f18ea6eb423e029c9dcaf7` (v0.11.0, schema 10).

Staging deployment `1a67d055-f042-4e52-a457-bbeeb6ba29f1` succeeded on this release. [Staging CI 34038793396](https://github.com/GreenShoeGarage/Oracle/actions/runs/34038793396) passed verification and the complete remote all-twelve/all-three-theme workflow with cleanup. Production deployment `42f6212a-27ad-4847-bc79-a08796bd162a` succeeded on the same commit. [production CI 34039312308](https://github.com/GreenShoeGarage/Oracle/actions/runs/34039312308) passed exact readiness plus 70 public GET paths without gameplay writes. Exact job links and evidence are in [STATUS.md](STATUS.md).

| Target | Railway project | Project ID | Application URL | Status |
| --- | --- | --- | --- | --- |
| Staging | ORACLE Staging | `4e75d5ec-f8e9-492d-ae75-e0d44428d24a` | [Staging app](https://oracle-production-488d.up.railway.app) | v0.11.0/schema 10 deployed; exact CI, complete all-twelve/all-three-theme workflow, 70 public GETs, and cleanup passed |
| Production | ORACLE | `d1989864-9a40-4176-a9d3-203a06c4bd72` | [Production app](https://oracle.greenshoegarage.com) | v0.11.0/schema 10 live; exact readiness and all 70 public GET checks passed |

Each project uses its own default Railway environment named `production`. These are separate environments in separate projects: staging has `APP_ENV=staging`, while production has `APP_ENV=production`. The environment IDs are `ae381b6c-578f-4e25-a71c-094124ca2105` for staging and `c31cbf3b-caa0-4c09-ae75-741daa6bcd85` for production.

Each has a separate PostgreSQL 18 service and persistent volume, using Railway's PostgreSQL template with `/var/lib/postgresql/data` mounted as required by that image. Application `DATABASE_URL` references resolve to the Postgres service in the same project and environment. Credentials and event data are isolated.

The canonical production origin is `https://oracle.greenshoegarage.com`, explicitly set in `APP_ORIGIN`. The generated [Railway production domain](https://oracle-production-77f2.up.railway.app) also serves health checks, but cookie-authenticated mutations require the canonical origin. Use the custom domain for normal application access.

## Batch 11 release gates

The release adds entry/next-step guidance, compact event actions, readability/focus/error recovery, and explicit local data/navigation safeguards. It changes no SQL migration, briefing/adventure format, browser archive version 2, or Batch 10 queued information/account-binding contract. Preserve the existing operator account and all schema-10 data.

Nine root DOM/API groups passed with zero uncaught errors. Guide/pilot/load-tool checks passed five/eight/four; the load-tool lifecycle uses two players and is not the 100-player measurement. The calculated color audit passed 31 pairs and eleven kit checks passed. Full local `npm run verify` passed 306 tests (294 passed, zero failures, 12 deliberate TCP-only skips), with complete footer, exit 0, and duration 145,383 ms. Syntax/version/static asset checks passed before the subsequent load-cleanup fix.

The first candidate (`9439760`) reached [main CI 34038258200](https://github.com/GreenShoeGarage/Oracle/actions/runs/34038258200), [verification job 101500202414](https://github.com/GreenShoeGarage/Oracle/actions/runs/34038258200/job/101500202414). Its PostgreSQL suite passed 306 tests (305 passed, zero failures, one PGlite-only skip), but the new isolated load runner failed during disposable database cleanup: the pool's end promise resolved before its backend connection fully closed, and forced database removal triggered an unhandled PostgreSQL `57P01`. No complete `LOAD_REHEARSAL_RESULT` was emitted, so this attempt provides no accepted capacity result. Recovery, startup, and Docker gates did not run. Neither staging nor production was advanced from Batch 10 for that failed attempt. The corrected runner waits for owned database connections to drain before plain removal, records unexpected pool errors safely, and passed all four focused load-tool checks; the subsequent corrected candidate `6127db477f76f1c3b9f18ea6eb423e029c9dcaf7` passed [main CI 34038628533](https://github.com/GreenShoeGarage/Oracle/actions/runs/34038628533) and [verification job 101501202501](https://github.com/GreenShoeGarage/Oracle/actions/runs/34038628533/job/101501202501) with 307 tests (306 passed, zero failures, one PGlite-only skip), all twelve TCP gates, the isolated load run, populated recovery, repeated migration/startup, and Docker secure-cookie/graceful-stop checks. The workload, environment guards, and release gates were unchanged.

Railway staging deployment `1a67d055-f042-4e52-a457-bbeeb6ba29f1` succeeded on `6127db477f76f1c3b9f18ea6eb423e029c9dcaf7` at 14:19:45 UTC on September 6. Startup confirmed schema 10 at 14:19:38 and v0.11.0 staging readiness at 14:19:43; the staging operator remains absent as configured. [staging CI 34038793396](https://github.com/GreenShoeGarage/Oracle/actions/runs/34038793396) and [verification job 101501643902](https://github.com/GreenShoeGarage/Oracle/actions/runs/34038793396/job/101501643902) passed 307 tests (306 passed, zero failures, one PGlite-only skip), all twelve TCP gates, populated recovery, repeated migration/startup, and the production image. The separate [remote workflow job 101501900225](https://github.com/GreenShoeGarage/Oracle/actions/runs/34038793396/job/101501900225) and overall staging run passed. Exact readiness plus 70 public GET routes, all twelve connected journeys in Fantasy/Cyberpunk/Wasteland, starter/field/revocation checks, and actual instrument/STAGEHAND copied play/reset passed; cleanup archived disposable events and signed out sessions at 14:28:40 UTC. Production then advanced to the same commit and passed its independent deployment/public gate.

The second isolated CI measurement also passed: 100 players/sessions/connections/peak server requests, 1,800 HTTP 200 responses, zero errors/timeouts, 4.176 seconds, p50 137.4 ms, p95 501.4 ms, maximum 767.46 ms, and 431.03 requests/second. It repeated all 100 effects/100 receipts/200 retries/100 unchanged inventories/100 denied cross-account reads, signed out 101 accounts, and completed server close, pool drain, database drop, and cleanup without errors. This is another short CI burst, not a deployed Railway load test.

A public desktop Chrome check loaded v0.11.0, verified the skip link targets the focusable main region, observed visible buttons at least 44 pixels high and no horizontal overflow, and found zero ORACLE console errors (four unrelated extension-metadata errors). It did not exercise authenticated browser tasks, physical phones, installation/offline controls, or actual 200% browser zoom.

Run exact-candidate main PostgreSQL CI first, including the new bounded 100-authenticated-player isolated load gate. Do not advance staging until main is green. The unchanged staging workflow must then pass all twelve instruments in all three themes, field retries, authorization changes, copied play/reset, and cleanup on that exact candidate. Only then advance production and record Railway success, exact readiness, and the actual public GET count. Batch 11 passed all 70 public GET paths, including the two new static assets.

The corrected candidate measured 100 distinct authenticated players in one disposable event: 100 sessions, 100 overlapping player connections, and 100 peak server requests. All 1,800 measured requests returned HTTP 200, with zero errors/timeouts, in 4.772 seconds (377.2 requests/second); p50 was 184.12 ms, p95 462.87 ms, and maximum 795.51 ms. Integrity checks found 100 effects, 100 receipts, 200 exact retries, 100 unchanged inventories, and 100 rejected cross-account reads. Cleanup archived the event, signed out all 101 accounts, closed the server, drained/dropped the owned database, and recorded no unexpected pool errors. The configuration was PostgreSQL 18.6, Node 22.23.2, four logical CPUs, and a ten-connection application pool. This short cohosted CI burst does not establish sustained load, Railway capacity, outdoor-network performance, or human/device acceptance.

The unchanged shared-IP authentication limit is 60 combined registration/login attempts per 15 minutes. For a large group behind one network address, pre-register and sign in ahead of time or stagger arrivals; creating accounts in advance alone does not remove the sign-in limit. The isolated load runner uses two observed loopback source-address groups during setup, not a relaxed application limit.

Railway production deployment `42f6212a-27ad-4847-bc79-a08796bd162a` succeeded on `6127db477f76f1c3b9f18ea6eb423e029c9dcaf7` at 14:30:01 UTC on September 6. Startup confirmed schema 10 at 14:29:50 and matched the existing operator. At 14:29:57, `server_ready` reported v0.11.0 production and `superuser_status` confirmed `accountExists: true`, `enabled: true`. [production CI 34039312308](https://github.com/GreenShoeGarage/Oracle/actions/runs/34039312308) and [public smoke job 101503054655](https://github.com/GreenShoeGarage/Oracle/actions/runs/34039312308/job/101503054655) passed exact commit/version/schema readiness at 14:30:06 and all 70 public GET paths through 14:30:17 (71 PASS lines including readiness). Verification made no production gameplay writes. `mike@greenshoegarage.com` remains the enabled superuser with identity/password preserved. The staging and production runtime branches retain this checked commit; final documentation changes are main-only.

Human/device checks remain unrun and require actual participants and devices. Use [PILOT.md](PILOT.md) and the local validator to record the 58 observations. Deploying the usability changes does not close those acceptance gates. Scheduled live backups remain outstanding under the current platform configuration.

## Previous verified release — Batch 10 gates

The complete local authenticated staging rehearsal passed 1,075 reads/1,155 writes using the actual staging script against a disposable HTTP/PGlite application. It retains all twelve instruments in all three themes and adds marked invitation/join/reading-offer replay, expected-account missing/malformed/mismatch checks, session invalidation and sign-in recovery, stale terms, restrictive sharing policy, current membership guards, rejected asset fields/preexisting trades, unchanged inventory/balances, explicit online confirmation, one-time receipts, and cleanup. It checks the server's original invitation deadline without extending it; it does not sleep through the full invitation lifetime. Actual local-queue expiry and lost-response/cross-tab behavior belong to controlled storage/integration fixtures. Local HTTP evidence is separate from deployed acceptance.

The focused information-only exchange suite passed seven tests; existing exchange coverage passed fourteen with one deliberate TCP-only skip. Storage/sync passed ten/five checks; independent HTTP/shared IndexedDB integration passed ten. Field desk/exchange UI passed eight/four, the actual root DOM/API walkthrough passed nine groups with zero uncaught errors, and transport passed three. Offline/cache, QR, and install modules passed twenty-one/fourteen/four checks (39 total), including legacy-archive upgrade/preservation. Full local `npm run verify` passed 290 tests (278 passed, zero failures, 12 deliberate TCP-only skips), with a complete exit-0 footer and syntax/version/static asset checks. Both exact-release GitHub/PostgreSQL 18.6 jobs passed 290 tests (289 passed, zero failures, one PGlite-only snapshot skip), all twelve TCP races, real populated schema-10 dump/restore, repeated migration, startup, and Docker secure-cookie/graceful-stop checks. Main run/job are `34013678298`/`101433703601`; staging run/verification job are `34013678237`/`101433703490`. Railway staging deployment `440e3c45-0684-4081-8b35-66d0ad3cf0b5` succeeded at 05:19:32 UTC on September 6 after schema-10 confirmation at 05:19:25 and v0.10.0 staging startup at 05:19:30. Its operator is unconfigured, as expected for the isolated staging project. The complete remote all-twelve/all-three-theme workflow passed on the same release and finished cleanup at 05:25:55 UTC. Production deployment `1e402c0f-5db1-4700-a97d-57678e31d498` succeeded at 05:28:12 UTC on the same release. At 05:28:04, startup confirmed migration history still at 10 and matched the existing operator. At 05:28:08, `server_ready` reported v0.10.0 production and `superuser_status` confirmed `accountExists: true`, `enabled: true`. [Production CI 34014040708](https://github.com/GreenShoeGarage/Oracle/actions/runs/34014040708), [job 101434651182](https://github.com/GreenShoeGarage/Oracle/actions/runs/34014040708/job/101434651182), passed exact readiness at 05:28:16 and all 68 public GET paths through 05:28:24 (69 PASS lines including readiness). Production verification made no gameplay writes. Both runtime branches retain `466b470eb6902fd84e903a0fbb1415b7f7a4fa8e`; final documentation updates are main-only. Physical two-device, representative installed-browser/camera, and human field-pilot tests remain outstanding; automated HTTP/DOM/IndexedDB tests do not satisfy them.

Batch 10 adds no SQL migration. Migrations 001–010 and briefing/adventure formats remain unchanged. The browser journal IndexedDB archive advances to version 2 with the same stores and retained readings; older version-1 writers are blocked. Reuse the populated schema-10 recovery fixture, including all 28 economy/instrument/operations tables, earlier data, all migration records, and the complete enabled operator. Do not add a migration merely for the version bump. Application compatibility must also preserve the `informationOnly`/expected-account API and immutable replay contract used by saved requests; v0.9 sharing the same schema is insufficient evidence for an app rollback.

## Previous verified release — Batch 9 gates

The local migration/foundation suite passed 39 tests (38 passed, zero failures, one existing TCP-only skip), preserving the complete enabled operator, all nine migration records, every schema-9 instrument table, and earlier account/event/economy/story state. The recovery fixture adds all five schema-10 tables, scoped readiness, exact party consent, an overdue dispatched party, a separate returned party with a fallback release receipt, and a published operational bulletin linked to its encounter revision. Populated constraints and repeated migration passed locally; actual schema-10 dump/restore passed in both exact-commit PostgreSQL CI runs.

The complete local authenticated staging rehearsal passed 971 reads/1,035 writes. It now exercises all twelve instruments in each of the three themes, including scoped staff preparation/privacy, preserved legacy scene capacity, whole-party consent, cancellation-before-redirect, readiness and event pauses, absolute dispatch deadlines and replay, approved/current BROADSIDE, explicit return, self-queue cancellation, actual copied operations play/news/reset, source isolation, and whole-event release. The remote staging job has a 15-minute bound; exact-commit readiness still waits at most five minutes. Cleanup archives disposable events and signs out sessions. No production scenario data is created.

Focused HTTP/browser checks and eight full-app DOM/API checks passed with zero uncaught errors. Full local verification passed 242 tests (230 passed, zero failures, 12 deliberate TCP-only skips). Both exact-commit GitHub/PostgreSQL jobs passed 242 tests (241 passed, zero failures, one PGlite-only snapshot skip), all twelve TCP gates (the existing eight plus four operations races), actual populated schema-10 recovery, startup, and Docker secure-cookie/graceful-stop checks. Railway staging deployment `29d0f779-c261-40bd-b344-9feda10edf7d` passed: migration 10 at 04:24:11 UTC and v0.9.0 staging startup at 04:24:17 UTC on September 6. The complete remote staging workflow passed every themed operations/copy-reset/event-end path and cleaned up at 04:30:13 UTC. Production deployment `7ec0e7d5-0ea6-46e7-8d12-8ece92552fc2` passed on the same release. Migration 10 and the matching existing operator were confirmed at 04:31:37 UTC. At 04:31:42 UTC, v0.9.0 production startup reported `accountExists: true`, `enabled: true`; Railway marked deployment successful at 04:31:45 UTC. Exact-commit readiness passed at 04:31:46 UTC and all 56 public GET checks passed through 04:32:23 UTC. Provisioning preserved the existing identity/password and production checks created no account or event. Runtime branches retain the checked release commit; the final documentation update is main-only. See [STATUS.md](STATUS.md) for exact run/job links.

## Previous verified release — Batch 8 gates

Batch 8 retained every previous authorization, adventure, exchange, story, economy, agreement, and recovery gate. Its migration/foundation suite passed 38 tests (37 passed, zero failures, one existing TCP-only skip), preserving all populated schema-8 rows and the complete eight earlier migration records. The local recovery fixture populates all ten new tables, including private/live publications, roles/components, completed outcomes, paused timers, fictional rules/overrides, and captured readings. The complete local staging rehearsal passed 392 reads/466 writes, including all earlier workflows, every-theme cooperation/signals, atomic component rollback and one-time deduction, and copied instrument play/reset. Focused SIGIL/STATIC HTTP and browser checks passed; ten full-app DOM/API checks passed with zero uncaught errors.

Full local `npm run verify` passed 217 tests (209 passed, zero failures, eight deliberate TCP-only skips). Release commit `ef3d822c0e6a36fd0f1e3081b68f5d750d268106` passed both main and staging PostgreSQL CI runs: 217 tests (216 passed, zero failures, one PGlite-only snapshot skip), all eight TCP gates, actual populated schema-9 pg_dump/restore, repeated migration, startup, and the production Docker image with secure cookies and graceful stop. The complete remote staging workflow passed in every theme, including final component rollback/once-only charge, staff controls, actual copied instrument play/reset, all prior workflows, and cleanup. Production promotion used that exact tested commit and passed all 52 independent public GET checks.

Staging migration 9 completed at 03:38:14 UTC on September 6 and v0.8.0 startup at 03:38:17 UTC. The remote workflow finished every themed path and cleanup at 03:41:09 UTC. Production logs confirmed migration 9 and the matching existing operator at 03:42:42 UTC; v0.8.0 startup reported `accountExists: true`, `enabled: true` at 03:42:46 UTC. Exact-commit production readiness passed at 03:42:49 UTC and all 52 public GET checks passed through 03:42:57 UTC. Provisioning preserved the existing identity/password; production verification created no account or event. Runtime branches retain the checked release commit; final documentation updates are main-only.

## Previous verified release — Batch 7 gates

The candidate local HTTP rehearsal passed 305 reads/342 writes, including previous three-theme adventures/exchanges/story and new finite-stock purchases, mixed asset barter, independent agreement witnessing, exact terms revisions, atomic settlement/retry, dispute/adjudication, linked correction, and real rehearsal reset. The 37-test migration/foundation set passed across the initial run and corrected-fixture rerun, with one existing TCP-only skip. All 13 new tables passed populated recovery-fixture constraints and repeat migration locally. Root integration checks passed 22 tests; kit/offline checks passed 27. Eight full-app DOM/API checks passed with zero uncaught errors and ten OATHBOOK UI scenarios passed. Economy HTTP passed seven tests, OATHBOOK thirteen, story thirteen, and exchange fourteen plus one TCP-only skip. Full local `npm run verify` passed 183 tests (179 passed, zero failures, four deliberate TCP-only skips), including syntax/version/static assets. Release commit `a008a659d4f2cc9425a88640bffd5f3df8774d2b` passed both main and staging CI: 183 PostgreSQL tests (182 passed, zero failures, one PGlite-only snapshot skip), all four TCP gates including forced last-stock and same-item contention, actual populated schema-8 dump/restore, startup, and the production Docker image. Railway staging and the complete deployed BAZAAR/QR/OATHBOOK/three-theme adventure/story workflow passed on that same commit. Railway production and all 43 exact-commit public checks passed on that same release, confirming v0.7.0/schema 8 and the existing enabled operator account.

Both the exact-candidate staging gate and the independent production gate passed. Migration 008 preserved populated schema-7 story and investigation state, every older record, and the existing enabled operator's full identity/credential row. CI now restores populated fictional resources, finite shops, transactions/receipt grants, initial inventory baselines, trade snapshots, and exact agreement signatures/history in addition to all prior tables. The remote workflow adds a disposable independent witness account. Production checks remain read-only.

Production logs confirmed migration 8 and the matching existing operator at 02:52:35 UTC on September 6. At 02:52:39 UTC, v0.7.0 startup reported `accountExists: true`, `enabled: true`. Exact-commit readiness passed at 02:52:43 UTC and all 43 public checks passed through 02:52:48 UTC. Provisioning preserved the existing identity/password; no production account or event was created. Runtime branches retain the checked source commit; the final documentation update is main-only.

## Repository and release branches

The owner-created repository is public and contains the source at its root. Its existing license and Git attributes have been preserved. Keep secrets out of Git.

Use `main` for integration and `staging` for the staging application. The workflow listens to `main`, `staging`, and `production` pushes and pull requests. Its `verify` job runs on integration/staging commits and pull requests. Batch 11 also runs the bounded 100-player disposable PostgreSQL measurement after the full suite, before recovery/startup/image checks. Check main on the exact candidate first, then advance staging only after that job is green. Batch 10 retains all prior gates and adds field storage/account isolation, explicit information-only replay, install/cache/update protection, and camera/print fallbacks. Batch 9 added scoped readiness, whole-party queues/consent/dispatch, authoritative capacity, explicit returns and lifecycle closure, current approved operational news, and operations-aware rehearsal reset. Migration rehearsals begin with all 23 populated schema-8/9 economy/instrument tables, earlier story/exchange/adventure/character data, all nine prior migration records, and the existing enabled operator. Recovery fixtures retain those records and populate every new STAGEHAND table. On `staging`, a dependent `staging-smoke` job waits for the checked `GITHUB_SHA` to appear in readiness with the expected application and schema versions, then runs the authenticated multi-account all-twelve/all-three-theme journey, including disposable witnesses and scoped staff. Production promotion skips duplicate verification and runs `production-smoke`: up to five minutes waiting for that exact SHA, version, and schema at the canonical custom domain, followed by public GET checks. This relies on promoting the already-verified commit; the production job is not a replacement for CI and staging gates. Railway production follows the dedicated `production` release branch, at checked release `6127db477f76f1c3b9f18ea6eb423e029c9dcaf7` with successful production deployment/public checks. Advance that branch only after checking CI and staging for the exact candidate commit. This is a manual promotion procedure: Railway's Wait for CI was unavailable through the connector and `checkSuites` remains false. No automatic branch protection is claimed.

The complete automated Batch 5 staging workflow passed on the recorded release commit: two players introduced characters, exchanged selected readings with mutual consent, exercised policy/offer changes and duplicate prevention, resumed persisted receipts/journals, and reset a rehearsal containing completed exchanges. All three prior adventure paths and existing event/character/QR workflows also passed, followed by cleanup. Main/staging CI passed 131 tests (130 passed, one PGlite-only snapshot skip), both TCP races, real populated-table dump/restore, startup, and the running production image. Production deployment and public-only checks also passed on the same commit; see [STATUS.md](STATUS.md).

The Batch 6 local HTTP staging rehearsal passed 252 reads and 287 writes, including every audience type, actual faction reapproval/removal, group removal, rumor QR sharing, private/shared investigations, publication correction/withdrawal, and discovery-gated rehearsal reset. The migration/foundation suite passed 35 tests with one existing TCP-only skip; the exact new recovery fixture passed local constraints and repeated migration. Full local verification passed 157 tests (155 passed, zero failures, two existing TCP-only skips). Ten full-app DOM/API checks passed with zero uncaught errors; fourteen story UI and ten TRACE UI focused checks also passed. The final audience-validation alignment then passed all eleven focused TRACE tests. Both exact-candidate CI runs passed PostgreSQL 18.6 with 156 tests passed and one PGlite-only skip, both TCP races, actual populated schema-7 dump/restore, startup, and the production image. The complete remote staging workflow passed and cleaned up its disposable data. Railway production and all 37 exact-commit public checks passed on that same release. Production startup confirmed the existing enabled operator account, preserving its identity and password. These local results do not replace the deployed gates.

Do not assume that a successful push proves deployment success. Inspect the workflow for the exact commit, then the Railway deployment result and readiness endpoint.

## Configure Railway before source attachment

The current deployment uses a separate staging project, with its own PostgreSQL instance, credentials, persistent volume, app instance, and domain. When recreating it, verify that staging's `DATABASE_URL` resolves to its staging database and that the PostgreSQL template's required data directory is on the mounted persistent volume.

For each application service, configure:

| Setting | Value |
| --- | --- |
| Build | Root `Dockerfile` |
| Start command | `npm start` |
| Pre-deploy command | `npm run migrate` |
| Healthcheck path | `/health/ready` |
| Healthcheck timeout | 300 seconds |
| Restart policy | On failure, maximum 3 retries |
| Application sleep | Disabled for live events |
| `NODE_ENV` | `production` |
| `APP_ENV` | `staging` or `production` |
| `DATABASE_URL` | `${{Postgres.DATABASE_URL}}`, referencing the database in this project/environment |
| `DATABASE_SSL` | `disable` for the intended Railway private network connection |
| `PORT` | `3000`, matching the generated domain's target port |
| `APP_ORIGIN` | Exact HTTPS origin for this app instance |
| `REGISTRATION_ENABLED` | `true` initially; configurable by operator |

The application binds `0.0.0.0` and honors Railway's `PORT`. Pre-deploy migrations have database access and do not depend on a mounted application filesystem. All migration files and the Node runtime are present in the final image. Configure a deployment/pre-deploy deadline using the platform controls available to your account; the SQL driver also bounds individual statements.

Railway's current documentation does not permit new services to opt into `railway.json` or `railway.toml`. This project therefore uses a Dockerfile and explicit service configuration. A future `.railway/railway.ts` must be applied through Railway's CLI; merely committing it does not configure a deployment. See the official [Infrastructure as Code documentation](https://docs.railway.com/infrastructure-as-code).

Attach the confirmed repository and correct environment branch only after database readiness, variables, migration command, and health checks are configured. If the connection workflow attaches and deploys immediately, use a source-less configured application service first and connect the source as the last step.

## Project superuser provisioning

Current production state (Batch 9): the reserved operator account is enabled. Migration and startup logs confirmed the matching existing account at this release; provisioning preserved its identity and password. No replacement user was created. The conditional instructions below remain the supported process for new installations or absent reserved accounts.

Provisioning is operator configuration for this installation, not a sign-up preference. Store these values only in protected Railway variables; do not put an operator's personal email or setup secret in the repository or an event pack.

1. Set `BOOTSTRAP_SUPERUSER_EMAIL` to the operator-selected account address. Use the same normalization as sign-in: trimmed, lowercase email.
2. If the account already exists, the configured pre-deploy `npm run migrate` promotes that same database user and enables it. Its UUID, password hash, and event data remain intact. Sign in with the existing password; no reset or replacement account is created.
3. If the account does not exist, migration reserves the address without creating a user. To claim it, configure a cryptographically random `BOOTSTRAP_SUPERUSER_SETUP_TOKEN` of 32–512 characters and provide it through the registration form's operator setup-code field. Registration must be open. Knowledge of the email alone cannot register the reserved superuser.
4. After a successful claim, remove the temporary setup-token variable. The unique existing account and ordinary password sign-in are sufficient. Keep the operator email setting only if deployment should continue to enforce that account's superuser/enabled state.
5. Verify the matching account can open Project administration and All events. The migration log reports matching/provisioning state. A read-only startup `superuser_status` record reports `accountExists` and `enabled`, allowing deployment verification even when pre-deploy logs are unavailable. Neither log prints the configured email, setup secret, or password. Do not infer provisioning succeeded from a set variable alone.

No account or privilege is inferred from an email in a browser request alone. Existing reserved accounts are provisioned by deployment; absent accounts require the separate operator-held secret. Public registration cannot select a role, and the admin interface has no role-grant or password-reset action. Leave both variables unset where project administration is not needed. Staging and production require their own operator configuration; do not reuse production secrets for disposable staging checks.

## Release gate

1. Run `npm ci` and `npm run verify` on the release candidate.
2. Pass the exact-candidate main `verify` job against PostgreSQL 18, including the bounded 100-authenticated-player isolated load measurement with recorded configuration/latency/errors/integrity/cleanup, prior-schema preservation, character/superuser authorization, QR encode/decode and camera teardown, concurrent credential/session tests, backup/restore of populated character, administration, adventure, exchange, story, investigation, economy, agreement, cooperative, fictional-reading, and operations records, and running production Docker image checks.
3. Deploy that candidate to the isolated staging database. The `staging-smoke` job waits up to five minutes for `/health/ready` to report the expected app version, schema, and exact `GITHUB_SHA` in `deploymentCommit`. A healthy older deployment does not satisfy this gate.
4. Pass the multi-account staging journey: event isolation, invitation redemption, roles, player/prop secret filtering, all three theme changes without record loss, organizer pack import as a separate Draft, player export filtering, stale-version rejection, persistence after a fresh sign-in, and immediate access removal. Retain character approval/privacy/inventory and identity checks. Retain both players' paths through the three complete adventures, release words, hints, correct/exhausted puzzle paths, overrides, reservation limits, idempotent actions, persistence, pause/revocation, and rehearsal-copy/reset isolation. Retain empty-offer introductions, selected bilateral copies, titles-only before both confirmations, changed offers/policies, rejection/cancellation/expiry, same-request replay, canonical-origin deduplication, receipt/contact/journal persistence, and exchange-aware rehearsal reset. For Batch 6 also pass different private rumor accounts, hidden-truth filtering, confirmed rumor QR transfer, citation/link visibility, manager exclusion from private theories, all four audiences and current affiliation/group revocation, proposal review, separate draft/live publication, correction/withdrawal, and remapped/reset rehearsal story state. For Batch 7 also pass explicit balances, corrected price/version, finite stock, purchase replay and failed-funds isolation, selected item/resource barter, revised consent, last-leg rollback, independent witnessing, exact revised acceptance, participant-only settlement/replay, dispute/adjudication without double spend, linked corrections, and reset of actual rehearsal purchases/agreements to initial inventory/stock. For Batch 8 also pass two-role/three-step cooperation in every theme, host/event pauses and explicit resume, unchanged heartbeat replay lease, immutable outcomes, final component failure rollback and once-only success consumption, cancellation/retry, reasoned staff override, fictional conditional/manual state changes, stale collection/replay, and actual copied instrument play/reset. For Batch 9 also run the full exchange/story/economy journey in every theme and pass scoped two-encounter readiness, current member assent, full-capacity rejection, preserved legacy attendance, scene cancellation followed by whole-party redirect, event/readiness pauses, absolute dispatch window and retry, explicit release, private player views, submitted/approved/stale operational news, actual copied operations/news reset, and event-end closure of waiting/dispatched parties. For Batch 10 also pass marked create/join/reading-offer replays, expected-account failures, invalidated sessions and renewed sign-in, stale revisions/current sharing policy, rejected asset-bearing offers, unchanged assets and once-only receipt copies, and membership denial of new and replayed requests in every theme. Preserve all prior cleanup. This is HTTP workflow verification; it does not replace browser/device or human field testing.
5. Review the migration, API, and recovery position. Batch 11 retains schema 10, pack formats 1, browser archive version 2, and the Batch 10 API contract; Batch 10 changed no SQL or stored pack format; retain the populated schema-10 recovery gate and the information-only/account-binding contract for queued retries. Migration 010 adds five STAGEHAND tables while preserving every schema-9 cooperative/fictional-reading record and schema-8 economy/agreement record and all earlier accounts, credentials, events, setup, characters, inventories, memberships, invitations, adventure histories, sharing policies, completed exchanges, copy provenance, receipts, contacts, story publications/collections, and private investigations. No live database backup or scheduled backup is currently available through the configured platform; record this unresolved limitation. Event-pack exports do not protect account or membership data. Prefer a verified protected database checkpoint when backup capability is available, and use the schema-10 roll-forward procedure below.
6. Advance the `production` branch to the exact checked source commit. Verify Railway deployment success, readiness, the deployment commit, public assets, and the session endpoint at the canonical production origin. The automated `production-smoke` job waits up to five minutes for the exact promoted commit and checks only public GET routes. It creates no production users or events.
7. Record the release commit, app version, migration version, deployment ID, successful CI/staging checks, and remaining operational limitations in [STATUS.md](STATUS.md).

To exercise the authenticated staging gate manually, set `EXPECTED_COMMIT` to the full 40-character candidate SHA and run `node scripts/staging-check.js`. It permits account/event mutations only at the hard-coded staging origin. Do not alter the allowlist to run disposable-account checks against production. The ordinary `SMOKE_ORIGIN='https://oracle.greenshoegarage.com' npm run smoke` is the public production check.

Railway's deployment health check gates traffic switching; it is not continuous uptime monitoring. See [Health checks](https://docs.railway.com/deployments/healthchecks), [Pre-deploy commands](https://docs.railway.com/deployments/pre-deploy-command), and [GitHub autodeploys](https://docs.railway.com/deployments/github-autodeploys).

## Backups

Scheduled production backups are **not configured**. Railway's returned effective limits for this HOBBY workspace report `maxBackupsCount: 0`. No paid plan change or external backup service has been configured. The PostgreSQL `pg_dump`/`pg_restore` rehearsal passed in GitHub CI on disposable test data; it verifies the recovery tooling and does not protect live production data.

Before relying on ORACLE for event data, enable and verify scheduled volume backups on a plan that supports them, or configure a scheduled external backup runner with protected credentials and durable storage. Verify retention and a real restore. Scheduled backups remain an outstanding Batch 1 operational requirement until configured and verified.

For a portable logical backup, use PostgreSQL client tools matching or newer than the server major version. Set `PGHOST`, `PGPORT`, `PGUSER`, `PGPASSWORD`, and `PGDATABASE` in the backup runner's protected environment. Keep backups outside the app container and out of Git.

```bash
umask 077
pg_dump --format=custom --no-owner --no-acl --file=oracle-backup.dump
pg_restore --list oracle-backup.dump
```

An archive listing checks readability; it is not a restoration test. Rehearse a full restore into a newly created, isolated database and compare the important records. Use `scripts/recovery-rehearsal.js` with fresh `oracle_test*` source/target databases to exercise the process automatically. The script refuses to overwrite an existing target database. For GitHub CI, it uses the PostgreSQL service container's matching client tools.

ORACLE's production Node image does not contain PostgreSQL client utilities. Run backup tooling from a dedicated runner/container that can reach the database, or use the platform backup controls. Never enable a public backup download route.

## Application rollback and database recovery

Migration 010 adds `stagehand_encounters`, `stagehand_parties`, `stagehand_requests`, `stagehand_history`, and `stagehand_announcements`. Migrations 001–009 remain unchanged. Existing accounts/credentials, event setup, characters/inventories, adventures/journals, policies/exchanges, story publications/collections, investigations, all 13 economy/agreement tables, all ten SIGIL/STATIC tables, and audit history are retained. Existing events receive no seeded encounter, party, readiness, or operational notice during migration; only newly created starter adventures receive an unlinked planning encounter. Migration checksums and transactions remain enforced; no destructive down migration is provided.

**After schema 10 is applied, the schema-9 v0.8.0 application is not a compatible rollback image.** Earlier releases are also incompatible. Do not deploy an older binary against this database, bypass readiness, or drop operations/instrument/economy/agreement tables to force startup. Preserve data and roll forward with a tested schema-10-compatible fix. Subsequent app-only rollbacks require verified compatibility with the current schema, stored formats, and queued request/API contracts. In particular v0.9 also uses schema 10 but does not implement v0.10 information-only/account-bound retries. Preserve those semantics in a tested roll-forward fix. A database restore remains a separate incident decision.

An application deploy also builds a complete versioned public service-worker cache. The install helper reports completed offline preparation and requests explicit update application with an unsaved-work warning. Other active tabs retain compatible assets and are not forcibly reloaded. Verify `/sw.js`, the manifest/icons, every allowlisted public module/CSS asset, matching shell-version headers, and the database-backed API on the exact release. An incomplete cache must not replace a complete shell; no API, secret definition, user credential, redirect, or query response belongs in it. Field desk data persists separately with account/generation guards. Include offline/update checks on representative real browsers in the device rehearsal; public GET checks alone cannot prove installed behavior.

If incident recovery requires restoring a database: pause writes, take a current checkpoint where possible, restore a known verified backup into a separate target, inspect records and schema, verify a compatible application, switch the intended connection, and check authenticated workflows. A restore can discard changes since the backup and requires an explicit incident decision. No verified live production backup is currently recorded, so database restoration is not an available recovery promise for this deployment.

Official references: [Environments](https://docs.railway.com/environments), [PostgreSQL](https://docs.railway.com/databases/postgresql), [Backups](https://docs.railway.com/volumes/backups), [Deployment actions](https://docs.railway.com/deployments/deployment-actions).
