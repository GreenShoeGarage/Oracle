import '/app-v15.js';

// Batch 16 intentionally keeps project integrations inside the existing
// Community Projects workspace instead of adding another top-level instrument.
document.documentElement.dataset.oracleVersion='1.6.0';
